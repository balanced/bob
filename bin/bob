#!/usr/bin/env python
from __future__ import unicode_literals

import click
import logging
import sys

from bob import commands, configure_logging


@click.group()
@click.option('--logging-conf', default=None)
def cli(logging_conf):
    configure_logging(logging_conf)


commands.add_commands(cli)


if __name__ == '__main__':
    cli()
